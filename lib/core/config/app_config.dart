/// Global configuration flags for the application.
library;

/// Flag to check if Firebase is available.
/// Defaults to false and is set to true on successful initialization.
bool isFirebaseAvailable = false;
